import { Button } from "@chakra-ui/react";

const CancelButton = () => {
  return (
    <Button
      colorScheme={"blackAlpha"}
      color={"black"}
      size="sm"
      variant={"solid"}
    >
      Cancel
    </Button>
  );
};

export default CancelButton;
